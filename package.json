{
  "name": "pick-cli",
  "version": "0.0.1",
  "main": "index.js",
  "devDependencies": {
    "bun-types": "^1.0.6"
  },
  "scripts": {
    "build": "bun build index.ts --compile && mv ./index ./gpick"
  }
}
